# Contributing

Keep frontend changes in `Courses App/` and backend changes in `Spring Rest/`. Do not commit local MySQL credentials, generated frontend builds, or Maven build output.

Before submitting a change, run the affected component locally and keep the frontend API base URL and backend port aligned.
